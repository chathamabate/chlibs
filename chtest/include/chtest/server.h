
#ifndef CHTEST_SERVER_H
#define CHTEST_SERVER_H

// Messages of some sort will be sent to the server over RPC, the reconstructed and 
// added into some sort of log...
// Ultimately, the log will be translated into JSON and written to a file.
// Maybe it could always exist as JSON?? Eh, that's no fun..
// I don't care all that much about efficiency, more correctness IMO.

typedef struct _test_server_state_t {
    // What will the test server state actually hold??
    //
    // Can always print out server state after???

    // A running test will have what exactly?
    // A list a messages??
    // What will a message look like??
    // Hmmmmmmmm

} test_server_state_t;

#endif
