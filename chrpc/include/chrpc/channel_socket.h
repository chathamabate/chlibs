
#ifndef CHRPC_CHANNEL_SOCKET_H
#define CHRPC_CHANNEL_SOCKET_H

#include "chrpc/channel.h"

/*
typedef struct _channel_socket_t {

} channel_socket_t;

channel_status_t chn_s_max_msg_size(channel_socket_t *chn_s, size_t *mms);
channel_status_t chn_s_send(channel_socket_t *chn_s, const void *msg, size_t len);
channel_status_t chn_s_refresh(channel_socket_t *chn_s);
channel_status_t chn_s_incoming_len(channel_socket_t *chn_s, size_t *len);
channel_status_t chn_s_receive(channel_socket_t *chn_s, void *buf, size_t len, size_t *readden); 
*/

#endif
